apiVersion: apps/v1
kind: Deployment
metadata:
  name: free5gc-ausf
  labels:
    app: free5gc-ausf
spec:
  replicas: 1
  selector:
    matchLabels:
      app: free5gc-ausf
  strategy:
    type: RollingUpdate
  template:
    metadata:
      labels:
        app: free5gc-ausf
    spec:
      dnsPolicy: ClusterFirst
      restartPolicy: Always
      terminationGracePeriodSeconds: 30
      containers:
      - name: free5gc-amf
        image: succassion/free5gc-compose_free5gc-ausf
        imagePullPolicy: IfNotPresent
        command: ["/bin/sh", "-ec"]
        args: ["sleep 5 && ./ausf -ausfcfg ../config/ausfcfg.yaml"]
        ports:
        - name: free5gc-ausf
          containerPort: 29509
          protocol: TCP
        env:
        - name: GIN_MODE
          value: release
        resources:
          requests:
            memory: "32Mi"
            cpu: "150m"
          limits:
            memory: "32Mi"
            cpu: "150m"
        volumeMounts:
        - mountPath: /free5gc/config/ausfcfg.yaml
          name: ausfcfg
          subPath: ausfcfg.yaml
        - mountPath: /free5gc/config/free5gc.yaml
          name: free5gc
          subPath: free5gc.yaml
      volumes:
      - name: ausfcfg
        configMap:
          name: free5gc-ausf-configmap
          items:
          - key: ausfcfg.yaml
            path: ausfcfg.yaml
      - name: free5gc
        configMap:
          name: free5gc-ausf-configmap
          items:
          - key: free5gc.yaml
            path: free5gc.yaml

